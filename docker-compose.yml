services:
  app:
    build: .
    restart: unless-stopped
    env_file: .env
    ports:
      - "127.0.0.1:5001:5001"
    volumes:
      - ./backend/data:/app/backend/data
      - ./backend/logs:/app/backend/logs
